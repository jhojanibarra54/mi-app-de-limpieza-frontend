export const environment = {
  production: true,

  /**
   * `apiUrl`: La URL base del backend (API) para el entorno de producción.
   */
  apiUrl: 'https://dodgerblue-quetzal-341299.hostingersite.com/api' // <-- ¡IMPORTANTE! Reemplaza esto con la ruta real a tu carpeta API en Hostinger
};
